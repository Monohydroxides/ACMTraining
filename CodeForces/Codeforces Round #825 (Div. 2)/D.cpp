#include <bits/stdc++.h>
using namespace std;

void work () {
    int n;
    string str;
    cin >> n >> str;
    str = " " + str;
    
}

int main () {
    cin.tie(0), cout.tie(0);
    ios::sync_with_stdio(false);
    int T;
    cin >> T;
    while (T--) {
        work();
    }
    return 0;
}