#include <bits/stdc++.h>
using namespace std;

int que[200010];

void work () {
    int n;
    cin >> n;
    string t;
    cin >> t;
    t = " " + t;
    
}

int main () {
    cin.tie(0), cout.tie(0);
    ios::sync_with_stdio(false);
    int T;
    cin >> T;
    while (T--) {
        work();
    }
    return 0;
}
